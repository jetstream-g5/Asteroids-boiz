package  
{
	import flash.display.Bitmap;
	import flash.display.MovieClip;
	import flash.display.Sprite;
	import flash.events.Event;
	import flash.geom.Point;
	
	/**
	 * ...
	 * @author
	 */
	 
	public class Enemy extends MovieClip
	{
		//public var targetPos:Point;
		//private var enemy:Array;
		//private var xmove:Number;
		//private var ymove:Number;
		//private var player:Player;
		//private var speed:Number;
		
		/*public function Enemy() 
		{
			player = new Player();
			
			this.rotation = player.x;
			this.speed = 5;
			this.x += this.rotation;
			
		}*/
		
		/*public function Enemy(startX:Number, startY:Number)
		{
			x = startX;
			y = startY;
			
		}
		
		public function moveDownABit():void
		{
			y = y + 3;
		}
		*/
		
		public function Enemy()
		{
			/*targetPos = new Point();
			enemy = new Array();
			Math.atan2(0.6, 0.8);
			
			var rad:Number = this.rotation * Math.PI / 180;
			xmove = Math.cos(rad);
			ymove = Math.sin(rad);
			*/
		}
		
		/*public function loop(e:Event):void
		{
			targetPos.x = Main.player.x - this.x;
			targetPos.y = Main.player.y - this.y;
		}
		*/
	}
}